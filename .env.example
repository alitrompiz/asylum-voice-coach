# Force HashRouter instead of BrowserRouter (useful for hosts without SPA rewrites, or staging safety)
# VITE_USE_HASH_ROUTER=false

# Disable PWA/Service Worker entirely (emergency switch)
# VITE_ENABLE_PWA=false
